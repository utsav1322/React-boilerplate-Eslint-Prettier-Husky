/*
 * Stpl Messages
 *
 * This contains all the text for the Stpl container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Stpl';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Stpl container!',
  },
});
